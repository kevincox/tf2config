// Note: Add -usercon to TF2 flags.

exec rcon_password
sv_lan 0 // Doesn't actually work for this server, but ensures that it is clear for next server start.
sv_rcon_banpenalty 1440 // min (24*60)

tf_use_fixed_weaponspreads 1
tf_weapon_criticals 0
