bind "HOME" "hud_reloadscheme"
bind "END" "hidepanel specgui"

// Make sure these are bound if we overwite them (ex: cheats).
bind "t" "say"
bind "y" "say_team"

voice_enable 1

alias starttalk "+voicerecord"
alias stoptalk  "-voicerecord"

bind "=" starttalk
bind "-" stoptalk
